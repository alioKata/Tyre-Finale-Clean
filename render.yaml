services:
  - type: web
    name: aliproject
    runtime: python
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: pip install -r requirements.txt && pip install gdown && chmod +x build.sh && ./build.sh
    envVars:
      - key: PORT
        value: 8000
      - key: SECRET_KEY
        generateValue: true 